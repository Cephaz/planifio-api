import {TranslationObject} from '../../../types';

const fr: TranslationObject = {
  serverError: 'Une erreur est survenue',
  emailUsed: 'Un utilisateur avec cette adresse email existe déjà',
};

export default fr;
