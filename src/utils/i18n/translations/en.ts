import {TranslationObject} from '../../../types';

const en: TranslationObject = {
  authFailed: 'Unable to authenticate',
  serverError: 'Something went wrong',
  loginFailure: 'Unable to login with the provided credentials',
  emailUsed: 'A user with this email already exists',
  logoutSucceed: 'Logged out successfully',
};

export default en;
